import React from "react";
import PersonIndex from "./PersonIndex";

/**
 * Stránková komponenta pro správu osob
 * Slouží jako tenká obálka nad PersonIndex
 */
const PersonPage = () => {
  return <PersonIndex />;
};

export default PersonPage;
